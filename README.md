# ACL
A config language

## Features
- explicit types
- arrays (type[])
- named/repeated blocks (interface "eth0" { ... })
- expressions, casts, and references ($ = global, $. = local, ^ = parent)
- semicolons required